class RollBar {
  float x,y,width,height;
  boolean isEnd = false;
  
  RollBar(float x,float y,float width,float height){
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
  }
}